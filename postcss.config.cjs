module.exports = {
    plugins: [
        require('@tailwindcss/postcss'), // Use the new Tailwind PostCSS plugin
        require('autoprefixer'),        // Ensure autoprefixer is included if needed
    ],
};
